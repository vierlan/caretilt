<% @packages.each do |package|%>
  <h2><%= package.name %></h2>
  <p><%= package.price %></p>
  <p><%= package.validity %></p>
  <%= button_to "buy Now!", stripe_checkout_path(price_id: price.id), data: {turbo: 'false'} %>
  <% end %>
