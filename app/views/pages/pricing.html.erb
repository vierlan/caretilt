<div class="py-10">
  <div class="mx-auto max-w-7xl px-6 lg:px-8">
    <div class="mx-auto max-w-4xl text-center">
      <p class="mt-2 text-page-heading">Pricing plans for teams of&nbsp;all&nbsp;sizes</p>
    </div>
    <p class="mx-auto mt-6 max-w-2xl text-center text-lg leading-8 text-gray-600">Choose an affordable plan that’s packed with the best features for engaging your carers, creating customer loyalty, and driving sales.</p>
    <div class="mt-16 flex justify-center">
      <fieldset aria-label="Payment frequency">
        <div class="grid grid-cols-2 gap-x-1 rounded-full p-1 text-center text-xs font-semibold leading-5 ring-1 ring-inset ring-gray-200">
          <!-- Checked: "bg-indigo-600 text-white", Not Checked: "text-gray-500" -->
          <label class="cursor-pointer rounded-full px-2.5 py-1">
            <input type="radio" name="frequency" value="monthly" class="sr-only">
            <span>Monthly</span>
          </label>
          <!-- Checked: "bg-indigo-600 text-white", Not Checked: "text-gray-500" -->
          <label class="cursor-pointer rounded-full px-2.5 py-1">
            <input type="radio" name="frequency" value="annually" class="sr-only">
            <span>Annually</span>
          </label>
        </div>
      </fieldset>
    </div>
    <div class="isolate mx-auto mt-10 grid max-w-md grid-cols-1 gap-8 md:max-w-2xl md:grid-cols-2 lg:max-w-4xl xl:mx-0 xl:max-w-none xl:grid-cols-4">
      <div class="rounded-3xl p-8 ring-1 ring-gray-200">
        <h3 id="tier-hobby" class="text-lg font-semibold leading-8 text-gray-900">Starter</h3>
        <p class="mt-4 text-description-small" style="min-height: 220px">
        Small or new providers who are likely to have less than 3 services and rarely have a void / vacancy. Referrals are managed by 1 person and typically work with fewer than 3 Local Authorities.
        </p>
        <p class="mt-6 flex items-baseline gap-x-1">
          <!-- Price, update based on frequency toggle state -->
          <span class="text-4xl font-bold tracking-tight text-gray-900">£360</span>
          <!-- Payment frequency, update based on frequency toggle state -->
          <span class="text-sm font-semibold leading-6 text-gray-600">/annum</span>
        </p>
        <a href="/stripe/checkout" aria-describedby="tier-hobby" class="mt-6 block rounded-md px-3 py-2 text-center text-sm font-semibold leading-6 text-indigo-600 ring-1 ring-inset ring-indigo-200 hover:ring-indigo-300 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">Buy plan</a>

        <% starter_plan_features = ["1 Credit", "1 User", "Added to Caretilt database"] %>
        <ul role="list" class="mt-8 space-y-3 text-description-small">
          <% starter_plan_features.each do |feature| %>
            <%= render partial: "shared/pricing_list_item", locals: { content: feature } %>
          <% end %>
        </ul>
      </div>
      <div class="rounded-3xl p-8 ring-1 ring-gray-200">
        <h3 id="tier-freelancer" class="text-lg font-semibold leading-8 text-gray-900">Lite</h3>
        <p class="mt-4 text-description-small" style="min-height: 220px">
            Small providers of 4 or more services who are likely to have less than 1 void / vacancy per annum. Referral team is 1 or 2 people and typically work with fewer than 5 Local Authorities but have regular transitions.
        </p>
        <p class="mt-6 flex items-baseline gap-x-1">
          <!-- Price, update based on frequency toggle state -->
          <span class="text-4xl font-bold tracking-tight text-gray-900">£1500</span>
          <!-- Payment frequency, update based on frequency toggle state -->
          <span class="text-sm font-semibold leading-6 text-gray-600">/annum</span>
        </p>
        <form action="/stripe/checkout" method="POST">
        <!-- Add a hidden field with the lookup_key of your Price -->
        <input type="hidden" name="lookup_key" value="{{PRICE_LOOKUP_KEY}}" />
        <a href="#" aria-describedby="tier-enterprise" class="mt-6 block rounded-md px-3 py-2 text-center text-sm font-semibold leading-6 text-indigo-600 ring-1 ring-inset ring-indigo-200 hover:ring-indigo-300 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">Buy plan</a>
      </form>
        <% lite_features = ["10 Credits", "Up to 3 Users", "Added to Caretilt database", "Ability to switch void/vacancy on and off as required", "Added to Local Authority bulletin", "Can receive direct messages from Local Authorities"] %>
        <ul role="list" class="mt-8 space-y-3 text-description-small">
          <% lite_features.each do |feature| %>
            <%= render partial: "shared/pricing_list_item", locals: { content: feature } %>
          <% end %>
        </ul>
      </div>
      <div class="rounded-3xl p-8 ring-2 ring-indigo-600">
        <div class="flex items-center justify-between gap-x-4">
          <h3 id="tier-startup" class="text-lg font-semibold leading-8 text-indigo-600">Pro</h3>
          <p class="rounded-full bg-indigo-600/10 px-2.5 py-1 text-xs font-semibold leading-5 text-indigo-600">Most popular</p>
        </div>
        <p class="mt-4 text-description-small" style="min-height: 220px">
        Medium providers of 10 or more services who are likely to have more than 3 voids a annum. Small referral team or managed by registered managers . Typically work with more than 5 Local Authorities and probably specialist.
        </p>
        <p class="mt-6 flex items-baseline gap-x-1">
          <!-- Price, update based on frequency toggle state -->
          <span class="text-4xl font-bold tracking-tight text-gray-900">£3000</span>
          <!-- Payment frequency, update based on frequency toggle state -->
          <span class="text-sm font-semibold leading-6 text-gray-600">/annum</span>
        </p>
        <a href="#" aria-describedby="tier-startup" class="mt-6 block rounded-md bg-indigo-600 px-3 py-2 text-center text-sm font-semibold leading-6 text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">Buy plan</a>

        <% pro_features = ["50 Credits", "Up to 5 Users", "Added to Caretilt database", "Ability to switch void/vacancy on and off as required", "Added to Local Authority bulletin", "Can receive direct messages from Local Authorities", "Full onboarding service", "Receive and respond to Local Authority placement bids"] %>
        <ul role="list" class="mt-8 space-y-3 text-description-small">
          <% pro_features.each do |feature| %>
            <%= render partial: "shared/pricing_list_item", locals: { content: feature } %>
          <% end %>
        </ul>
      </div>
      <div class="rounded-3xl p-8 ring-1 ring-gray-200">
        <h3 id="tier-enterprise" class="text-lg font-semibold leading-8 text-gray-900">Unlimited</h3>
        <p class="mt-4 text-description-small" style="min-height: 220px">
            Medium to Large providers who have regular voids/ vacancies. Referral team typically handles more than 10 referrals a annum and work with more than 10 Local Authorities
        </p>
        <p class="mt-6 flex items-baseline gap-x-1">
          <!-- Price, update based on frequency toggle state -->
          <span class="text-4xl font-bold tracking-tight text-gray-900">£6000</span>
          <!-- Payment frequency, update based on frequency toggle state -->
          <span class="text-sm font-semibold leading-6 text-gray-600">/annum</span>
        </p>
        <a href="#" aria-describedby="tier-enterprise" class="mt-6 block rounded-md px-3 py-2 text-center text-sm font-semibold leading-6 text-indigo-600 ring-1 ring-inset ring-indigo-200 hover:ring-indigo-300 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">Buy plan</a>
        <% unlimited_features = ["Unlimited Credits", "Unlimited Users", "Added to Caretilt database", "Ability to switch void/vacancy on and off as required", "Added to Local Authority bulletin", "Can receive direct messages from Local Authorities", "Full onboarding service", "Receive and respond to Local Authority placement bids"] %>
        <ul role="list" class="mt-8 space-y-3 text-description-small">
          <% unlimited_features.each do |feature| %>
            <%= render partial: "shared/pricing_list_item", locals: { content: feature } %>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
