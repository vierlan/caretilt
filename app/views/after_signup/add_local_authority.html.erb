<div class="min-h-screen flex justify-center py-12 px-8 sm:px-6 lg:px-8">
  <div class="max-w-md w-full">
    <%= render 'devise/shared/devise_heading', title: 'Sign up' %>
    <%= render "devise/shared/devise_sub_heading", subtitle: "Let's get to know you." %>
    <p>Please enter correct details for invoicing.</p>
    <%= form_for @local_authority, url:wizard_path, method: :put do |f| %>
      <div class="mb-2">
        <%= f.text_field :name, class: "my-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-primary-500 focus:ring focus:ring-primary-500 focus:ring-opacity-50", placeholder: "Authority/Organisation name", required: true %>
      </div>
      <div class="mb-2">
        <%= f.text_field :email, class: "my-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-primary-500 focus:ring focus:ring-primary-500 focus:ring-opacity-50", placeholder: "Email", required: true %>
      </div>
      <div class="mt-6">
        <%= f.submit "Next", class: "group relative w-full flex justify-center py-2.5 px-4 border border-slate-600 text-xl leading-5 font-medium rounded-md text-black bg-slate-200 hover:bg-primary focus:outline-none bg-gradient-to-r from-secondary to-primary hover:bg-gradient-to-r from-primary to-secondary" %>
      </div>
    <% end %>
  </div>
</div>
